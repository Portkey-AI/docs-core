---
title: Create Image Edit
openapi: post /images/edits
---

<Note>
Image edit requests use `multipart/form-data` and can include large binary files. Portkey does not rewrite provider-prefixed model aliases inside multipart payloads, so pass the upstream provider's model or deployment name directly in the `model` form field instead of values like `@provider/model-name`.
</Note>
