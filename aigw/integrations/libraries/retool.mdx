---
title: 'Retool'
description: 'Connect Prisma AIRS AI Gateway as a Custom AI Provider in Retool to access 3,000+ LLMs with observability and governance'
---

Retool is a low-code platform for building internal apps and automations. Use the AI Gateway as a Custom AI Provider in Retool to route requests through 3,000+ LLMs, add observability, and enforce governance controls — all without changing your Retool queries.

## Prerequisites

- [AI Gateway API key](https://stratacloudmanager.paloaltonetworks.com/)
- Provider configured in [Model Catalog](https://stratacloudmanager.paloaltonetworks.com/)
- Retool account with resource creation permissions

<Note>
See [Retool's Custom AI Provider docs](https://docs.retool.com/data-sources/guides/connect/custom-ai-provider/resource) for more on resource setup.
</Note>

## Setup

<Steps>
<Step title="Configure the AI Gateway">
1. Go to [Model Catalog](https://stratacloudmanager.paloaltonetworks.com/) → **Add Provider**.
2. Select your provider (OpenAI, Anthropic, etc.), enter your API key, and create a slug like `openai-prod`.
3. Go to [API Keys](https://stratacloudmanager.paloaltonetworks.com/) → **Create new key** → Save.
</Step>

<Step title="Create a Custom AI Provider resource in Retool">
1. In Retool, go to **Resources** → **Create new** → **Resource**.
2. Search for **Custom AI Provider** and select it.
3. Give the resource a name (e.g., `Portkey`).
</Step>

<Step title="Configure the connection">
Fill in the following fields:

| Field | Value |
|-------|-------|
| **Base URL** | `https://aigw.portkey.ai/v1` |
| **API Key** | Your AI Gateway API key |
| **Compatible Schema** | OpenAI |
| **Model Names** | Use AI Gateway Model Catalog slugs (e.g., `@openai-prod/gpt-5`, `@anthropic-prod/claude-opus-4-7`) |

The AI Gateway model slugs follow the format `@<provider-slug>/<model-id>`, where the provider slug is the one you created in Model Catalog. This routes each request to the right provider and model automatically — no extra headers needed.

</Step>

<Step title="Save and verify">
Click **Create resource**. To verify, create a query in your app or workflow, select the AI Gateway resource, choose **Generate chat response**, and run it.

<Note>
Custom AI Provider resources don't have a **Test connection** button. Verify by running a query.
</Note>
</Step>
</Steps>

## Use the AI Gateway in queries

In any Retool app or workflow, create a new query and select your AI Gateway resource. Choose an action type:

| Action | Description |
|--------|-------------|
| **Generate text** | Content generation, code generation, summarization |
| **Generate chat response** | Multi-turn conversations with message history |
| **Summarize text** | Condense documents or feedback |
| **Classify text** | Categorize inputs into predefined labels |
| **Extract entity from text** | Pull structured data from unstructured text |

### Example: Chat query

Set up a query with **Generate chat response** and use dynamic Retool expressions in your prompt:

```
System message:
You are a helpful assistant for our internal support team.

Prompt:
Analyze this customer feedback: {{ feedbackTextarea.value }}
```

## Advanced headers

Add these as custom headers on the resource for more control:

| Header | Value | Purpose |
|--------|-------|---------|
| `x-portkey-config` | Your Config ID | Enable fallbacks, retries, caching |
| `x-portkey-metadata` | `{"_user": "{{ currentUser.email }}"}` | Tag requests with user info |

## What you get with the AI Gateway

- **3,000+ LLMs** — Switch between OpenAI, Anthropic, Google, Mistral, and more from the same Retool resource
- **Observability** — Every query logged with latency, cost, and token usage in the [Strata Cloud Manager](https://stratacloudmanager.paloaltonetworks.com/)
- **Reliability** — Automatic retries and fallbacks keep your Retool apps running
- **Governance** — Budget limits, rate limits, and RBAC per team or workspace
- **Guardrails** — PII detection and content filtering on requests and responses

---

import AdvancedFeatures from '/snippets/aigw/portkey-advanced-features.mdx';

<AdvancedFeatures />
