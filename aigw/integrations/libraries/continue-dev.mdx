---
title: 'Continue.dev'
description: 'Add enterprise-grade observability, cost tracking, and governance to your Continue.dev AI coding assistant'
---

Continue.dev is an open-source AI coding assistant for VS Code and JetBrains. Add Prisma AIRS AI Gateway to get:

- **3,000+ LLMs** through one interface - switch providers instantly
- **Observability** - track costs, tokens, and latency for every request
- **Reliability** - automatic fallbacks, retries, and caching
- **Governance** - budget limits, usage tracking, and team access controls

This guide shows how to configure Continue.dev with the AI Gateway in under 5 minutes.

<Note>
  For enterprise deployments across teams, see [Enterprise Governance](#3-enterprise-governance).
</Note>

---

## Video Walkthrough

<Frame caption="Setting up Continue.dev with the AI Gateway - full walkthrough">
  <iframe
    width="100%"
    height="400"
    src="https://drive.google.com/file/d/1hbmdqMJvzchDkS207JPYPdUvUaHwt-9a/preview"
    title="Continue.dev + AI Gateway Integration"
    allow="autoplay"
    allowFullScreen
  />
</Frame>

---

# 1. Setup

<Steps>
<Step title="Add Provider">
Go to [Model Catalog](https://stratacloudmanager.paloaltonetworks.com/) → **Add Provider**.

</Step>

<Step title="Configure Credentials">
Select your provider (OpenAI, Anthropic, Vertex AI, Mistral, etc.), enter your API key, and create a slug like `openai-prod`.

</Step>

<Step title="Get AI Gateway API Key">
Go to [API Keys](https://stratacloudmanager.paloaltonetworks.com/) and generate your AI Gateway API key.
</Step>
</Steps>

---

# 2. Configure Continue.dev

<Steps>
<Step title="Open config.yaml">
In VS Code, open the Continue sidebar with `Cmd/Ctrl + L`. Click the agent/model dropdown at the top of the chat input, hover over **Local Config**, and click the **gear icon** ⚙️.

This opens your local config file at:
- `~/.continue/config.yaml` (macOS / Linux)
- `%USERPROFILE%\.continue\config.yaml` (Windows)

</Step>

<Step title="Add AI Gateway Configuration">
Paste the configuration for your provider. Continue treats the AI Gateway as an OpenAI-compatible endpoint.

<Tabs>
  <Tab title="OpenAI (GPT-4o-mini)">
```yaml
name: My Config
version: 0.0.1
schema: v1

models:
  - name: "Portkey - GPT-4o-mini"
    provider: openai
    model: gpt-4o-mini
    apiBase: "https://aigw.portkey.ai/v1"
    apiKey: "YOUR_PORTKEY_API_KEY"
    requestOptions:
      headers:
        x-portkey-provider: "@openai-prod"
    roles:
      - chat
      - edit
      - apply
```
  </Tab>
  <Tab title="Anthropic (Claude Opus 4.6)">
```yaml
name: My Config
version: 0.0.1
schema: v1

models:
  - name: "Portkey - Claude Opus 4.6"
    provider: openai
    model: claude-opus-4-6
    apiBase: "https://aigw.portkey.ai/v1"
    apiKey: "YOUR_PORTKEY_API_KEY"
    requestOptions:
      headers:
        x-portkey-provider: "@anthropic-prod"
    roles:
      - chat
      - edit
      - apply
```
  </Tab>
  <Tab title="Google Vertex AI">
```yaml
name: My Config
version: 0.0.1
schema: v1

models:
  - name: "Portkey - Gemini 2.5 Pro (Vertex)"
    provider: openai
    model: gemini-2.5-pro
    apiBase: "https://aigw.portkey.ai/v1"
    apiKey: "YOUR_PORTKEY_API_KEY"
    requestOptions:
      headers:
        x-portkey-provider: "@vertex-prod"
    roles:
      - chat
      - edit
      - apply
```

<Note>
  For Claude models on Vertex AI, use the Vertex model ID format: `anthropic.claude-opus-4@20250514` and point `x-portkey-provider` to your Vertex AI provider slug.
</Note>
  </Tab>
  <Tab title="Mistral">
```yaml
name: My Config
version: 0.0.1
schema: v1

models:
  - name: "Portkey - Mistral Large"
    provider: openai
    model: mistral-large-latest
    apiBase: "https://aigw.portkey.ai/v1"
    apiKey: "YOUR_PORTKEY_API_KEY"
    requestOptions:
      headers:
        x-portkey-provider: "@mistral-prod"
    roles:
      - chat
      - edit
      - apply
```
  </Tab>
</Tabs>

Replace:
- `YOUR_PORTKEY_API_KEY` → your key from Step 1
- `@openai-prod` / `@anthropic-prod` / `@vertex-prod` / `@mistral-prod` → your actual provider slug from Model Catalog
</Step>

<Step title="Select the Model in Continue">
Save `config.yaml`. Continue auto-reloads on save. In the sidebar dropdown, switch from **Default Assistant** to **Local Config**, then pick your AI Gateway model from the list.

<Note>
  If the model does not appear, ensure you selected **Local Config** in the agent selector — not the default assistant.
</Note>
</Step>
</Steps>

Done! Monitor usage in the [Strata Cloud Manager](https://stratacloudmanager.paloaltonetworks.com/).

---

## Switch Providers

Change models by updating the **model** field and `x-portkey-provider` header in `config.yaml`:

```
@openai-prod/gpt-4o-mini
@anthropic-prod/claude-opus-4-6
@vertex-prod/gemini-2.5-pro
@mistral-prod/mistral-large-latest
```

All requests route through the AI Gateway automatically.

## Add Multiple Models

You can define multiple models in `config.yaml` and switch between them from the Continue sidebar at any time:

```yaml
models:
  - name: "Portkey - GPT-4o-mini"
    provider: openai
    model: gpt-4o-mini
    apiBase: "https://aigw.portkey.ai/v1"
    apiKey: "YOUR_PORTKEY_API_KEY"
    requestOptions:
      headers:
        x-portkey-provider: "@openai-prod"
    roles:
      - chat
      - edit
      - apply

  - name: "Portkey - Claude Opus 4.6"
    provider: openai
    model: claude-opus-4-6
    apiBase: "https://aigw.portkey.ai/v1"
    apiKey: "YOUR_PORTKEY_API_KEY"
    requestOptions:
      headers:
        x-portkey-provider: "@anthropic-prod"
    roles:
      - chat
      - edit
      - apply

  - name: "Portkey - Gemini 2.5 Pro"
    provider: openai
    model: gemini-2.5-pro
    apiBase: "https://aigw.portkey.ai/v1"
    apiKey: "YOUR_PORTKEY_API_KEY"
    requestOptions:
      headers:
        x-portkey-provider: "@vertex-prod"
    roles:
      - chat
      - edit
      - apply

  - name: "Portkey - Mistral Large"
    provider: openai
    model: mistral-large-latest
    apiBase: "https://aigw.portkey.ai/v1"
    apiKey: "YOUR_PORTKEY_API_KEY"
    requestOptions:
      headers:
        x-portkey-provider: "@mistral-prod"
    roles:
      - chat
      - edit
      - apply
```

<Note>
**Want fallbacks, load balancing, or caching?** Create a [AI Gateway Config](/aigw/product/ai-gateway/configs), attach it to your API key, and set `x-portkey-config` to your Config ID in `requestOptions.headers`. See [Enterprise Governance](#3-enterprise-governance) for examples.
</Note>

---

import AdvancedFeatures from '/snippets/aigw/portkey-advanced-features.mdx';

<AdvancedFeatures />
