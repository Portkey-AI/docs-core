---
title: Guardrails
description: Apply policies to MCP requests.
---

Guardrails apply policies to MCP requests before and after they execute.

## Available now

**Rate limiting.** Limit requests and token consumption per API key, server, or tool. See [MCP Rate Limits](/product/mcp-gateway/rate-limits).

**Guardrail checks.** Run the same guardrail checks available for LLM requests against MCP tool calls, before and after they execute. Configure checks per-server or per-workspace using the standard [guardrails workflow](/product/guardrails/list-of-guardrail-checks). Denied calls return a structured error containing the guardrail and check ID.

## What's coming

**Approval workflows.** Require explicit approval for high-risk operations like deletions or write actions.

**Budget controls.** Limit usage based on cost or request volume.

## Get early access

Contact us if you're interested in early access to approval workflows or budget controls.


import PrismaAirsCta from "/snippets/prisma-airs-cta.mdx";

<PrismaAirsCta />
